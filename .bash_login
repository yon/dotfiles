[ -n "${DEBUG}" ] && echo "... starting .bash_login";

[ -r ${HOME}/.bashrc ] && . ${HOME}/.bashrc;

# use sane control characters
/bin/stty sane
/bin/stty erase 

# ssh key management
if [ -r ${HOME}/.ssh/id_rsa ]; then
    if [ `${TYPE_CMD} keychain` ]; then
        `${TYPE_CMD} keychain` --agents ssh --inherit any --quiet id_rsa fb_id_rsa;
        # `${TYPE_CMD} keychain` --agents ssh,gpg --inherit any --quiet id_rsa fb_id_rsa A3EEC242;
    fi
fi

if [ -d ${HOME}/.home ]
then
    make -s;
fi

[ -n "${DEBUG}" ] && echo "... ending .bash_login";
