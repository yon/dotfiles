[ -n "${DEBUG}" ] && echo "... starting .bash_environment";

GLOBIGNORE='..';
export GLOBIGNORE;

[ -r ${HOME}/.env ] && . ${HOME}/.env;

[ -n "${DEBUG}" ] && echo "... ending .bash_environment";

# vim:ft=sh:
