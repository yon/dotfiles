[ -n "${DEBUG}" ] && echo ">>> `basename $0`";

GLOBIGNORE='..';
export GLOBIGNORE;

if [ ${TERM} != "screen" ]; then
    PS1='[\h:\w] ';
else
    PS1='[${WINDOW}:\h:\w] ';
fi

export PS1;

[ -r ${HOME}/.env ] && . ${HOME}/.env;

[ -n "${DEBUG}" ] && echo "<<< `basename $0`";
